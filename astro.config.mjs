// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Needed by OG / canonical meta tags so they can resolve to absolute URLs.
  site: 'https://madebysam.dev',
});
