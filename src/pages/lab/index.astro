---
import Base from '../../layouts/Base.astro';

// Each experiment is a node on the map. mapPosition.{x,y} are normalised
// viewport coords (0..1, y measured from the TOP). Edit freely; the shader
// will follow.
type Category = 'three-d' | 'generative' | 'data' | 'audio' | 'tools';
type Status = 'live' | 'sketching' | 'parked';

interface Experiment {
  id: string;
  title: string;
  slug?: string;
  href?: string;
  category: Category;
  status: Status;
  note?: string;
  mapPosition: { x: number; y: number };
}

const experiments: Experiment[] = [
  {
    id: 'dvd-shelf',
    title: 'DVD Shelf',
    href: 'https://dvd.madebysam.dev',
    category: 'three-d',
    status: 'live',
    note: 'A 3D shelf for your physical media. Drop in a CSV, browse in space.',
    mapPosition: { x: 0.75, y: 0.34 },
  },
  {
    id: 'topo-playground',
    title: 'Topo Playground',
    category: 'generative',
    status: 'sketching',
    note: 'Same shader as this background, with knobs.',
    mapPosition: { x: 0.24, y: 0.46 },
  },
  {
    id: 'palette-generator',
    title: 'Palette Generator',
    category: 'tools',
    status: 'sketching',
    note: 'Throw a photo at it, get a colour system out.',
    mapPosition: { x: 0.78, y: 0.72 },
  },
  {
    id: 'audio-reactive',
    title: 'Audio Reactive',
    category: 'audio',
    status: 'sketching',
    note: 'Mic input drives a shader — volume, frequency, beat.',
    mapPosition: { x: 0.4, y: 0.8 },
  },
];

// Edges of the network. Each pair is a path drawn between the two nodes.
const connections: [string, string][] = [
  ['dvd-shelf', 'topo-playground'],
  ['dvd-shelf', 'palette-generator'],
  ['topo-playground', 'audio-reactive'],
  ['palette-generator', 'audio-reactive'],
  ['topo-playground', 'palette-generator'],
];

const categoryLabels: Record<Category, string> = {
  'three-d': '3D',
  'generative': 'Generative',
  'data': 'Data',
  'audio': 'Audio',
  'tools': 'Tools',
};

// Serialise data for the in-page script
const topoData = {
  peaks: experiments.map((e) => ({
    x: e.mapPosition.x,
    y: e.mapPosition.y,
    falloff: 110,
    strength: 0.45,
  })),
  segments: connections.map(([aId, bId]) => {
    const a = experiments.find((e) => e.id === aId)!;
    const b = experiments.find((e) => e.id === bId)!;
    return {
      a: { x: a.mapPosition.x, y: a.mapPosition.y },
      b: { x: b.mapPosition.x, y: b.mapPosition.y },
      falloff: 35,
      strength: 0.35,
    };
  }),
};
---

<Base title="lab — made by sam">
  <section class="lab-intro">
    <h1>Lab</h1>
    <p>A place to play. Experiments from the Obsidian vault, in public.</p>
  </section>

  <div class="lab-map" aria-label="Experiments map">
    {experiments.map((exp) => {
      const isLive = exp.status === 'live';
      const href = exp.href ?? (exp.slug ? `/lab/${exp.slug}` : undefined);
      const style = `left: ${exp.mapPosition.x * 100}%; top: ${exp.mapPosition.y * 100}%;`;
      const inner = (
        <>
          <span class="node__dot" aria-hidden="true" />
          <div class="node__label">
            <span class="node__title">{exp.title}</span>
            <span class="node__meta">
              <span>{categoryLabels[exp.category]}</span>
              {!isLive && <span class="node__status">{exp.status}</span>}
            </span>
            {exp.note && <span class="node__note">{exp.note}</span>}
          </div>
        </>
      );
      return isLive && href ? (
        <a class={`node node--${exp.status}`} style={style} href={href}>{inner}</a>
      ) : (
        <div class={`node node--${exp.status}`} style={style} aria-disabled="true">{inner}</div>
      );
    })}
  </div>
</Base>

<script define:vars={{ topoData }}>
  function applyNetworkMode() {
    const path = window.location.pathname.replace(/\/+$/, '') || '/';
    if (path !== '/lab') return;
    if (!window.__topo) return;
    window.__topo.setMode('locked', {
      peaks: topoData.peaks,
      segments: topoData.segments,
      noiseScale: 0.35,
      peakAA: 0,
      strength: 1,
    });
  }
  document.addEventListener('astro:page-load', applyNetworkMode);
  applyNetworkMode();
</script>

<style>
  .lab-intro {
    position: fixed;
    top: var(--space-8);
    left: 50%;
    transform: translateX(-50%);
    text-align: center;
    z-index: 2;
    pointer-events: none;
  }
  .lab-intro h1 {
    font-size: clamp(2rem, 5vw, 3rem);
    margin-bottom: var(--space-1);
  }
  .lab-intro p {
    color: var(--color-fg);
    opacity: 0.7;
    font-size: 0.95rem;
  }

  .lab-map {
    position: fixed;
    inset: 0;
    z-index: 1;
    pointer-events: none;
  }

  .node {
    position: absolute;
    transform: translate(-50%, -50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-2);
    color: inherit;
    text-decoration: none;
    pointer-events: auto;
    cursor: pointer;
    /* Light entrance — peaks rise out of the shader morph, labels fade in */
    opacity: 0;
    animation: node-in 0.6s ease-out forwards;
    animation-delay: 0.8s;
  }
  .node[aria-disabled='true'] {
    cursor: default;
    opacity: 0;
    animation: node-in-dim 0.6s ease-out forwards;
    animation-delay: 0.8s;
  }

  @keyframes node-in {
    from { opacity: 0; transform: translate(-50%, -45%); }
    to   { opacity: 1; transform: translate(-50%, -50%); }
  }
  @keyframes node-in-dim {
    from { opacity: 0; transform: translate(-50%, -45%); }
    to   { opacity: 0.6; transform: translate(-50%, -50%); }
  }

  .node__dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--color-fg);
    box-shadow: 0 0 0 4px var(--color-bg), 0 0 14px var(--color-bg);
    transition: transform var(--transition), background var(--transition);
  }
  .node:hover .node__dot {
    transform: scale(1.4);
    background: var(--color-accent);
  }
  .node[aria-disabled='true']:hover .node__dot {
    transform: none;
    background: var(--color-fg);
  }

  .node__label {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    text-align: center;
    max-width: 14rem;
    text-shadow:
      0 0 8px var(--color-bg),
      0 0 16px var(--color-bg),
      0 0 24px var(--color-bg);
  }
  .node__title {
    font-size: 1.05rem;
    font-weight: 500;
    transition: color var(--transition);
  }
  .node:hover .node__title { color: var(--color-accent); }
  .node[aria-disabled='true']:hover .node__title { color: inherit; }

  .node__meta {
    display: flex;
    gap: 8px;
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    opacity: 0.7;
  }
  .node__status { color: var(--color-accent); opacity: 0.9; }

  .node__note {
    font-size: 0.8rem;
    color: var(--color-fg);
    opacity: 0.7;
    max-width: 12rem;
    line-height: 1.4;
  }

  @media (max-width: 720px) {
    .lab-intro { top: calc(var(--space-8) - 1rem); }
    .node__label { max-width: 9rem; }
    .node__title { font-size: 0.95rem; }
    .node__note { font-size: 0.75rem; max-width: 9rem; }
  }
</style>
